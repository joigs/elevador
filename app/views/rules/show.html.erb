<h1><%= @rule.point %></h1>
<p>
  <strong>Comprobacion:</strong>
  <%= @rule.ruletype.gygatype_number %>
  <%= @rule.ruletype.rtype %>
  <br> <br>
  <strong>Clasificación del ascensor: </strong>
  <p> Grupo N°:
  <%= @rule.groups.pluck(:number).join(', ') %>
  </p>
<p>
  <strong>Nivel de importancia: </strong>
  <%= @rule.level %>
</p>
<p>
  <strong>Tipo de defecto:</strong>
  <%= display_rule_ins_type(@rule) %>
</p>




<div>
  <%= link_to 'Modificar defecto', edit_rule_path(@rule) %>
</div>

<div>
  <%= link_to 'Eliminar defecto', rule_path(@rule), data:
    {turbo_method: :delete,
     turbo_confirm: 'Estás seguro?'} if Current.user&.admin %>
</div>

<div>
  <%= link_to 'Volver a defectos', rules_path %>
</div>