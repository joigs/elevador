


<div id="<%= dom_id item %>" class="max-w-4xl mx-auto p-6 bg-gray-900 rounded-lg shadow-md hover:shadow-lg transition-shadow duration-300">
  <h2 class="text-xl font-bold text-gray-100 mb-4 break-words"><%= item.identificador %></h2>

  <div class="text-gray-300 space-y-2">
    <p>Clasificación: <%= item.group.name %></p>
    <p>Empresa: <%= link_to "#{item.principal&.name}", principal_path(item.principal), class:"text-blue-500", data: { turbo_frame: "_top" } %></p>

    <% inspection = last_inspection(item) %>
    <% if inspection %>
      <p>Última inspección: <%= format_date(inspection.ins_date) %></p>
      <p>Estado: <%= inspection.result %></p>
    <% else %>
      <p>Aun no hay inspecciones.</p>
    <% end %>
  </div>

  <% if (controller_name == 'items' && action_name == 'index') || (controller_name == 'principals' && action_name == 'show') %>
    <div class="mt-4">
      <%= link_to 'Ver más detalles', item_path(item), 'data-turbo-frame': "_top", class: "inline-block px-6 py-2 border border-blue-800 text-blue-400 font-medium text-xs leading-tight uppercase rounded hover:bg-blue-800 hover:text-white focus:outline-none focus:ring-0 transition duration-150 ease-in-out", 'data-turbo-frame': "_top" %>
    </div>
  <% end %>
</div>
