<p style="color: green"><%= notice %></p>

<%= render @ruletype %>

<div>

  <%= link_to 'Eliminar tipo de regla', ruletype_path(@ruletype), data:
    {turbo_method: :delete,
     turbo_confirm: 'Estás seguro?'} if Current.user&.admin %>  </p>


  <%= link_to "Volver atrás", ruletypes_path %>


</div>
