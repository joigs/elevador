<%= form_with(model: @revision) do |form| %>
  <% if item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(item.errors.count, "error") %> No se pudo guardar la revisión:</h2>

      <ul>
        <% @revision.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% @revision.group.rules do |rule| %>
    <p>
      <strong>Defecto:</strong>
      <%= rule %>
    </p>
    <!-- Add more fields as needed -->
  <% end %>

<% end %>
