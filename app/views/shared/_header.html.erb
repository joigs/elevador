<%= link_to home_path, class: "text-xl font-bold text-gray-900 hover:text-gray-700" do %>
  <h1 class="text-3xl font-semibold">Chilena de Certificaciones</h1>
<% end %>

<div class="flex justify-between items-center mt-4 px-4">
  <div class="bg-gray-800 p-2 rounded-lg flex justify-between w-full">
    <div>
      <%= link_to 'Usuarios', users_path, class: "text-blue-500 hover:text-blue-700" if Current.user&.admin %>
    </div>
    <div class="flex">
      <%= link_to 'Mi perfil', perfil_path(Current.user.username), class: "text-green-500 hover:text-green-700" if Current.user %>
      <%= link_to 'Cerrar sesión', session_path(Current.user.id), data: { turbo_method: :delete, turbo_confirm: 'Confirmar' }, class: "text-red-600 hover:text-red-800 ml-4" if Current.user %>
    </div>
  </div>
</div>
