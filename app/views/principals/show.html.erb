<p style="color: green"><%= notice %></p>

<%= render @principal %>
<div>
  <h3>Empresas menores asociadas</h3>
  <%= @principal.minors.map { |minor| link_to minor.name, minor_path(minor) }.join(', ').html_safe %>
</div>
<br>
<br>
<div>
  <%= link_to "Modificar empresa mandante", edit_principal_path(@principal) if Current.user.admin %>
</div>


<div>
  <%= link_to 'Eliminar empresa mandante', principal_path(@principal), data:
    {turbo_method: :delete,
     turbo_confirm: 'Estás seguro?'} if Current.user.admin? %>
</div>

<div>

  <%= link_to "Volver a empresas mandantes", principals_path %>
</div>


