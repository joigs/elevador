
<div class="max-w-4xl mx-auto sm:px-6 lg:px-8 py-8 bg-gray-800 rounded-lg shadow-md">
  <h1 class="text-2xl font-bold text-gray-100 mb-6 text-center">Modificar Empresa <%= @principal.name %></h1>

  <div class="flex justify-center my-6 space-x-4">
    <%= link_to "Volver atrás", principal_path(@principal),
                class: "text-md text-gray-300 hover:text-gray-400 transition duration-150 ease-in-out px-4 py-2 border border-gray-700 rounded hover:bg-gray-700" %>
    <% if Current.user.empresa == nil %>

      <%= link_to "Volver a empresas", principals_path,
                  class: "text-md text-blue-400 hover:text-blue-500 transition duration-150 ease-in-out px-4 py-2 border border-blue-500 rounded hover:bg-blue-800"%>
    <% end %>
  </div>

  <%= render "form", principal: @principal %>




</div>
